import 'package:flutter/material.dart';

class JamaahPage extends StatelessWidget {
  const JamaahPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Jamaah Page'),
      ),
    );
  }
}
